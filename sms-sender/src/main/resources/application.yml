ip:
  allowIp: 127.0.0.1
server:
  port: 8081
  context-path: /
spring:
  application:
    name: sms-sender
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/smsSender?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    username: root
    password: 970389
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

